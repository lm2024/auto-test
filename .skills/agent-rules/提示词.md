帮我编写一套通用Agent执行宪法，适配Claude Code、Kilo、UpCode，适配128K上下文、通义千问使用。
规则要点：
1. 文档仅用一级标题的轻量MD，仅单个换行分段，单份原子任务文档最大600行，超限必须拆分，拆分后内容互不重叠；
2. 所有文件平铺在根目录，禁止新建文件夹；文件命名统一为：两位数字需求号-两位数字主任务号-两位数字原子任务号-任务名称.md，并标注编号含义与示例；
3. 任务分三级：整体大需求→主任务→最小原子任务，一份文档对应一个原子任务；代码文件、模块严格隔离，禁止修改范围冲突；
4. 一个会话仅执行一个原子任务，不同任务分会话执行；有依赖任务按前置到后置顺序执行，无依赖可并行；
5. 严禁压缩、摘要、截断任何内容；仅读取当前任务必要文件，不全局索引项目；任务完成建议关闭会话、新建会话执行下一任务，解决上下文膨胀、运行卡顿问题；
6. 出现文档超限、目录违规、命名错误、任务冲突、顺序颠倒、内容压缩等问题，立即停止并提示异常。
请输出格式规整、可直接使用的完整宪法文本。