# 2026-06-27 工作日志

## 启动前后端服务
- 启动前端 (Vite v5.4.21) 在 `http://localhost:3001/`
- 启动后端 (Spring Boot 2.7.7) 在 `http://localhost:8080/`
- 后端 YAML 中 `server.port: 8080` 未生效（原因未排查），通过 `-Dspring-boot.run.arguments="--server.port=8080"` 强制指定端口
- 前端 npm install 需用 managed Node (22.22.2) 重新执行以修复 vite 符号链接
