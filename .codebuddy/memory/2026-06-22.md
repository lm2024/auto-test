# 2026-06-22

## Vue 3 → Vue 2.6 迁移完成

- 全部 12 个 `.vue` 文件从 `<script setup>` 转为 Options API
- 从 Vite 迁移到 Vue CLI + Webpack
- 所有可选链 `?.` 替换为 `&&` 兼容 Vue 2.6 模板编译器
- `npm run build` 构建成功，无错误
- 修复了 babel.config.js preset 路径问题
- 修复了 package.json 中 `@vue/cli-plugin-router` 版本不存在的问题（已移除该依赖）
