
将所有的页面从ep  转换为 antdesign   
同时完成如下需求
那么现在开始编写需求.  ant x6   我让你加了. 我不知道加哪里了...    我的需求是
链路编排 页面需要重新设计,重新架构; 引入ant x6  代替现有的流程编排. 要可以自由拖动. 自由配置属性
属性面板设计的过多, 排序号去掉. 可以自己拖动排序,不需要属性中配置
并行分组不要.去掉等待时间必须保留,很重要
请求配置摆放不合理, 需要紧凑点. 请求头可以格式化,但是请求体缺少格式化,并且缺少放大功能. 比如我需要填写的内容很多.所以这里可以放大查看编辑功能
请求体类型 只有三个吗? 够用吗?  需要你分析下
断言规则大部分测试能看懂. 提取规则看不懂所以这里需要重新考量. 我需要重新分析设计
最终的目的就是为了方便测试人员新增或修改的时候能容易理解,容易获取上一步返回的结果参数.喂给当前环节.
我们测试接口的时候会有前后接口联动的情况.虽然已经可以录制好.但手动增加的时候还是略显麻烦.所以编排的时候
应该能单独调用比如链路中的A节点. 单独访问下.页面会显示返回结果. 根据返回结果. 可以配置下一个节点的入参是否读上一个结果的返回结果的某个属性值
或者判断某个属性是否存在,或者根据返回的状态码,根据A节点返回结果.配置B节点入参,以此类推. 每个节点都能根据上一步的结果进行下去
同时需要有全局变量的配置.不如用户名,比如每个节点都有可能需要读的配置参数.等. 页面中需要有体现的地方.这里你帮我设计
链路编排页面中的节点库删掉,不要了!节点列表也不要了;
版本历史也不要了
链路编排旁边的输入框我不知道什么意思. 显示的是0   有没有用啊. 没用删了!  我希望页面的风格是易操作.易理解
让人一看就知道怎么操作. 所以要简洁. 复杂功能后台处理. 不需要展现那么多
节点库中的批量导入不要了.HTTP请求 发送HTTP请求   这几个字也不要了,
包括后端也删了.别留冗余代码
列表视图, 分组视图 , 我不知道ant x6 是否支持.  如果不支持就删了!    专注ant x6  设计架构好
我希望整个流程中能识别出url ,域名,  系统能自己识别到底是内部接口,还是外部接口! 内部接口ip或者域名都是可以在系统设置中可以配置的
这里如果表结构设计的不符合要求. 就重构! 表结构完全能重构. 架构设计要全面
我还希望增加个菜单.能展示整个系统的系统调用关系图!  根据捕获的链路. 分析系统调用关系. 这里需要依赖设置中的内部域名,  外部域名或ip等
系统调用关系图, 内部系统调用关系,调用分类,根据分类字典管理可以分析每个模块都涉及到哪些调用, 根据ip分析每个调用关系
根据模块分析等! 具备多种形式的图,表等展现方式.   你ant x6 到官网分析好都有哪些能做支持展现或者他不满足. 你要给我找个匹配的技术栈.现成的开源的大厂的技术栈
如果你有表结构的变更.要同时考虑到其他涉及到的代码.  都要改好. 必须能正常编译打包启动
后端代码要java8语法,jdk8语法!
系统关系图可以用ant g6 +  ECharts
有不会的就去查资料. 自己查. 自己解决
取用子任务完成, subagent  完成. 你负责监控
这改动比较大. 我希望你分批, 分步骤设计落地





ant x6  ECharts  ant g6 LogicFlow  jdk8  vue2 