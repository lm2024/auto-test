<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autotest.mapper.SysConfigMapper">

    <resultMap id="BaseResultMap" type="com.autotest.model.entity.SysConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="config_desc" property="configDesc"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT * FROM sys_config WHERE config_key = #{configKey}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM sys_config ORDER BY id
    </select>

    <insert id="insertOrUpdate" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_config (config_key, config_value, config_desc)
        VALUES (#{configKey}, #{configValue}, #{configDesc})
        ON DUPLICATE KEY UPDATE config_value = #{configValue}
    </insert>

</mapper>
