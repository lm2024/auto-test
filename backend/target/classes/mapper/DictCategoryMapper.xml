<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autotest.mapper.DictCategoryMapper">

    <resultMap id="BaseResultMap" type="com.autotest.model.entity.DictCategory">
        <id column="id" property="id"/>
        <result column="category_type" property="categoryType"/>
        <result column="category_code" property="categoryCode"/>
        <result column="category_name" property="categoryName"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO dict_category (category_type, category_code, category_name, sort_order, status)
        VALUES (#{categoryType}, #{categoryCode}, #{categoryName}, #{sortOrder}, 1)
    </insert>

    <update id="update">
        UPDATE dict_category
        <set>
            <if test="categoryName != null">category_name = #{categoryName},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM dict_category WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM dict_category WHERE id = #{id}
    </select>

    <select id="selectByType" resultMap="BaseResultMap">
        SELECT * FROM dict_category WHERE category_type = #{categoryType} AND status = 1 ORDER BY sort_order ASC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByType" resultType="int">
        SELECT COUNT(*) FROM dict_category WHERE category_type = #{categoryType} AND status = 1
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM dict_category WHERE status = 1 ORDER BY category_type, sort_order ASC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM dict_category WHERE status = 1
    </select>
</mapper>