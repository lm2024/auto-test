<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.autotest.mapper.TestAccountMapper">

    <resultMap id="BaseResultMap" type="com.autotest.model.entity.TestAccount">
        <id column="id" property="id"/>
        <result column="account_code" property="accountCode"/>
        <result column="account_name" property="accountName"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="product_code" property="productCode"/>
        <result column="system_name" property="systemName"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="auth_type" property="authType"/>
        <result column="auth_config" property="authConfig"/>
        <result column="login_type" property="loginType"/>
        <result column="login_config" property="loginConfig"/>
        <result column="login_script" property="loginScript"/>
        <result column="status" property="status"/>
        <result column="last_used_time" property="lastUsedTime"/>
        <result column="lock_until" property="lockUntil"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO test_account (account_code, account_name, tenant_id, product_code, system_name, username, password,
            auth_type, auth_config, login_type, login_config, login_script, status)
        VALUES (#{accountCode}, #{accountName}, #{tenantId}, #{productCode}, #{systemName}, #{username}, #{password},
            #{authType}, #{authConfig}, #{loginType}, #{loginConfig}, #{loginScript}, #{status})
    </insert>

    <update id="update">
        UPDATE test_account
        <set>
            <if test="accountName != null">account_name = #{accountName},</if>
            <if test="systemName != null">system_name = #{systemName},</if>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="authType != null">auth_type = #{authType},</if>
            <if test="authConfig != null">auth_config = #{authConfig},</if>
            <if test="tenantId != null">tenant_id = #{tenantId},</if>
            <if test="productCode != null">product_code = #{productCode},</if>
            <if test="loginType != null">login_type = #{loginType},</if>
            <if test="loginConfig != null">login_config = #{loginConfig},</if>
            <if test="loginScript != null">login_script = #{loginScript},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM test_account WHERE id = #{id}
    </delete>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM test_account WHERE id = #{id}
    </select>

    <select id="selectByAccountCode" resultMap="BaseResultMap">
        SELECT * FROM test_account WHERE account_code = #{accountCode}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT * FROM test_account
        <where>
            <if test="systemName != null and systemName != ''">
                AND system_name LIKE CONCAT('%', #{systemName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countList" resultType="int">
        SELECT COUNT(*) FROM test_account
        <where>
            <if test="systemName != null and systemName != ''">
                AND system_name LIKE CONCAT('%', #{systemName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <update id="lockAccount">
        UPDATE test_account
        SET status = 2, lock_until = #{lockUntil}, last_used_time = NOW()
        WHERE account_code = #{accountCode}
          AND (status = 1 OR (status = 2 AND lock_until IS NOT NULL AND lock_until &lt; NOW()))
    </update>

    <update id="releaseAccount">
        UPDATE test_account
        SET status = 1, lock_until = NULL
        WHERE account_code = #{accountCode}
    </update>

    <select id="selectAvailableByCode" resultMap="BaseResultMap">
        SELECT * FROM test_account
        WHERE account_code = #{accountCode}
          AND (status = 1 OR (status = 2 AND lock_until IS NOT NULL AND lock_until &lt; NOW()))
        LIMIT 1
    </select>

</mapper>